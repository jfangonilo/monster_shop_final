<h1 align= "center">Edit Review for <%= link_to @review.item.name, "/items/#{@review.item.id}" %></h1>
<center>
  <%= form_for [@item, @review], url: "/reviews/#{@review.id}" do |form| %>
      <%= render partial: 'form', locals: { f: form } %>
  <% end %>
</center>
